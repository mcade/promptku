<% provide(:title, 'Loved Promptkus') %>
<h1><%= @user.name %>'s most-loved promptkus</h1>
<div class="col-md-6 col-md-offset-3">

<%= render 'shared/public_feed' %>

<% unless (params[:page].present? || @microposts.count <25) %>
	<button class = "more">Show More</button>
<% end %>
	<%= paginate @microposts %>
</div>