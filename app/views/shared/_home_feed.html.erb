<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/home_feed_item', collection: @feed_items %>
  </ol>
 <% else %>
    <ol class="microposts">
 	  <span class="span8 noposts">There's nothing here yet. Check out <%= link_to "trending", microposts_path %> promptkus to find new and interesting people to follow.</span>
    </ol>
<% end %>

<script type="text/javascript"> 
    if ($( window ).width() > 855) {
        $('<link rel="stylesheet" type="text/css" href="//cf.shareasimage.com/static/app/css/publisher.css" >')
       .appendTo("head");
        $('i.fa-picture-o').css( "display", "inline" );
        $('span#picmobile').css( "display", "inline" );

        window.Sai = window.Sai || {};
        Sai.publisher = {
            publicKey: '4f592e01'
        };
        (function() {
            var se = document.createElement('script');
            se.type = 'text/javascript';
            se.async = true;
            se.src = '//cf.shareasimage.com/static/app/js/compiled/publisher.min.js';
            var lse = document.getElementsByTagName('script')[0];
            lse.parentNode.insertBefore(se, lse);
        })();
    }
</script> 

     


